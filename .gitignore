# build output
dist/
.astro/

# dependencies
node_modules/

# wrangler / miniflare local state
.wrangler/
worker-configuration.d.ts

# environment — never commit secrets
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# macOS / Windows cruft
.DS_Store
Thumbs.db

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# scratch
/tmp/
/scratch/

# editor AI rules — generated by the Codacy VS Code extension, not project source.
# Forward slashes: gitignore patterns are always POSIX paths, so the backslash
# form this started as matched nothing and the file kept showing up untracked.
.github/instructions/codacy.instructions.md
